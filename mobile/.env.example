# 10.0.2.2 is the special alias the Android emulator uses to reach
# the host machine's localhost. Use your machine's LAN IP instead
# when testing on a physical device (e.g. http://192.168.1.42:3000).
EXPO_PUBLIC_API_URL=http://10.0.2.2:3000
